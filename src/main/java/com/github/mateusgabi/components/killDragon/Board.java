package com.github.mateusgabi.components.killDragon;

public class Board implements IBoard {
//    matriz de peca

    @Override
    public void receiveData(IGetData dataProvider) {
//        this.matrizdePeca

        for (String datum : dataProvider.getData()) {
                // new

        }
    }
}
